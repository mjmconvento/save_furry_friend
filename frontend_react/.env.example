# Base URL of the Laravel API. Vite inlines VITE_* variables at build time, so
# this must be set for every build - src/config/api.ts throws if it is missing
# rather than silently falling back to localhost in a production bundle.
VITE_API_BASE_URL=http://localhost:8081
