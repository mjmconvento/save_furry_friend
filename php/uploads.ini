; The smaller of nginx client_max_body_size (32m) and PHP wins, so these must
; stay >= the nginx cap or uploads fail with a PHP-side 413 instead.
upload_max_filesize=32M
post_max_size=40M
memory_limit=256M
