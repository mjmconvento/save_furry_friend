; Loaded after www.conf (zz- prefix) so it appends to the existing [www] pool.
; The compose healthcheck probes this with cgi-fcgi (libfcgi-bin).
[www]
ping.path = /ping
ping.response = pong
pm.status_path = /status
